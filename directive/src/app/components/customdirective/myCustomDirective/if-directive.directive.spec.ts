import { IfDirectiveDirective } from './if-directive.directive';

describe('IfDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IfDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
